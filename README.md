# 💬 Angel Talk – Java Chat

> Java 소켓을 활용하여 **다중 클라이언트 채팅**을 구현한 학습용 프로젝트입니다.  
> `서버`와 `클라이언트`를 분리하여 네트워크 통신 구조와 멀티스레딩 개념을 직접 실습할 수 있도록 구성했습니다.

---

## 📌 프로젝트 소개 (Description)

**Java Chat (Angel Talk)** 은 다음과 같은 특징을 가진 간단한 채팅 애플리케이션입니다.

- TCP/IP 기반의 **클라이언트–서버 구조** 채팅 프로그램
- 여러 클라이언트가 동시에 접속하여 메시지를 주고받는 구조
- 서버/클라이언트를 **별도 프로젝트**로 분리하여 구조 이해에 용이
- 네트워크 프로그래밍 및 스레드 처리 기본 개념을 학습하기 위한 목적

---

## 🛠 기술 스택 (Tech Stack)

### 💻 Language & Runtime
- Java (JDK 8+ 또는 그 이상 권장)
- 표준 라이브러리
  - `java.net` – `ServerSocket`, `Socket` 을 사용한 TCP 통신
  - `java.io` – 입력/출력 스트림 기반 메시지 송수신
  - `java.lang`, `java.util` – 스레드 및 컬렉션 처리

### 🌐 Network & Architecture
- **TCP Socket 기반 클라이언트-서버 통신**
- **멀티스레드 서버**
  - 각 클라이언트 연결을 별도 스레드에서 처리
  - 메시지 브로드캐스트/중계 로직 구현 가능 구조

---

## ✨ 주요 기능 (Features)

### 🧩 서버 (java-chat-main-server)

- 클라이언트 연결 수락 (TCP 포트로 대기)
- 각 클라이언트 연결을 별도 스레드로 관리
- 클라이언트로부터 수신한 메시지를 다른 클라이언트에게 전달할 수 있는 구조
- 접속 / 종료 로그 출력 (콘솔 기반)

### 💬 클라이언트 (java-chat-main-client)

- 서버 IP / 포트에 연결
- 사용자가 입력한 메시지를 서버로 전송
- 서버로부터 전달받은 메시지를 수신하여 화면에 표시
- 여러 클라이언트를 실행하여 **채팅 테스트** 가능

> 실제 구현 상세(닉네임, 명령어 등)는 코드 기준으로 확장/수정하실 수 있습니다.

---

## 📁 프로젝트 구조 (Project Structure)

> 레포지토리 상위 구조 기준입니다.

```text
java_chat/
├── java-chat-main-client/      # 클라이언트 애플리케이션 프로젝트
├── java-chat-main-server/      # 서버 애플리케이션 프로젝트
└── angel_talk_logo.png         # Angel Talk 로고 이미지
````

* 각 디렉터리는 **독립적인 Java 프로젝트**로 구성되어 있으며,
  IDE(Eclipse 등)에 개별로 Import 하여 실행할 수 있습니다.

---

## 🚀 실행 방법 (How to Run)

### 1️⃣ 저장소 클론

```bash
git clone https://github.com/dangerouswomanlee/java_chat.git
cd java_chat
```

### 2️⃣ 서버 실행

1. IDE(Eclipse 등)에서 `java-chat-main-server` 프로젝트를 Import 합니다.
2. `main` 메서드를 포함한 **서버 진입점 클래스**를 실행합니다.

   * 예) `Server`, `ChatServer` 등 (실제 코드에서 확인 후 실행)

> 서버는 지정된 포트에서 클라이언트의 접속을 대기합니다.

### 3️⃣ 클라이언트 실행

1. IDE에서 `java-chat-main-client` 프로젝트를 Import 합니다.
2. `main` 메서드를 포함한 **클라이언트 진입점 클래스**를 실행합니다.
3. 서버와 동일한

   * **IP 주소** (예: `localhost`)
   * **포트 번호**
     를 입력하고 접속합니다.
4. 여러 개의 클라이언트 인스턴스를 실행하면,
   서로 다른 클라이언트끼리 메시지를 주고받는 것을 확인할 수 있습니다.

---

## 📚 학습 포인트 (What You Learn)

이 프로젝트를 통해 다음과 같은 내용을 학습/정리할 수 있습니다.

* Java에서의 **TCP 소켓 프로그래밍 기초**
* **클라이언트–서버 모델** 설계 방법
* `Thread` / `Runnable` 을 활용한 **멀티스레드 프로그래밍**
* 입출력 스트림(`InputStream`, `OutputStream`, `BufferedReader`, `PrintWriter` 등) 활용
* 네트워크 애플리케이션 테스트 및 디버깅 경험

---

## 🛠 TODO / 향후 개선 아이디어

* 사용자 **닉네임** 설정 및 접속/퇴장 알림 메시지
* `/quit`, `/whisper` 등 **채팅 명령어** 지원
* 채팅 기록 로그 파일 저장
* GUI(Swing / JavaFX) 클라이언트로 확장
* 방(채널) 개념 추가 및 다중 채팅방 지원
* 관리자 모드 추가

  
